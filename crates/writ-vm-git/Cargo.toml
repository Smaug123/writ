[package]
name = "writ-vm-git"
version = "0.1.0"
edition = "2024"

[dependencies]
writ-core = { path = "../writ-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
proptest = "1"
